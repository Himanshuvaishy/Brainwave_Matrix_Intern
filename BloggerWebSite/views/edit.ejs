<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Post</title>
    <link rel="stylesheet" href="/styles/style.css" />
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-lg">
        <p class="text-gray-600">Post Id:<span class="font-bold opacity-1 text-red-300"><%= post.id %></span> </p>
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Post Username: @<%= post.username %></h1>
        
        <form method="post" action="/posts/<%= post.id %>?_method=PATCH" class="space-y-4">
            <textarea rows="10" cols="35" name="content" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600">
                <%= post.content %>
            </textarea>
            <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-600">
                Submit
            </button>
        </form>
    </div>
</body>
</html>